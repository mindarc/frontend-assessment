BONUS ANSWER: 
---

`('b' + 'a' + + 'a' + 'a').toLowerCase()` results to `banana` because the + 'a' will result to 'NaN' or not a number since it coerce a string to a number. This will return baNaNa  and if used with the lower case funtcion, this will result to 'banana'


Instructions for Excercise 2
---
To run the excercise properly, install node.js then
run : npm install -g http-server 
finally run : http-server

*this is to read the local json file since browser is blocking the resource if loaded with no http
